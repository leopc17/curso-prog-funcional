dec pell : num -> num;
dec divide : num # num -> num;

--- pell 0 <= 0;
--- pell 1 <= 1;
--- pell n <= 2 * pell (n - 1) + pell (n - 2);

--- divide (n, d) <= if n >= d then 1 + divide (n - d, d) else 0;
